profiles {
    docker {
        docker.enabled = true
        singularity.enabled = false
        process.sifPath = ''
    }
    
    singularity {
        singularity.enabled = true
        process.sifPath = '~/empty/sifs'
        docker.enabled = false
        autoMounts = true
        runOptions = '--cleanenv --home ~/empty --no-home'
    }
}
